apiVersion: 1

providers:
  - name: 'GizmoSQL Sample Dashboards'
    orgId: 1
    folder: 'GizmoSQL'
    folderUid: 'gizmosql-samples'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
